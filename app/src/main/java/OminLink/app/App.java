/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package OminLink.app;

import OminLink.list.LinkedList;

import static OminLink.utilities.StringUtils.join;
import static OminLink.utilities.StringUtils.split;
import static OminLink.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
